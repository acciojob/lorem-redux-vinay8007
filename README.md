# Lorem Redux

A React + Redux web application built using Vite that fetches data from the Lorem Ipsum API
and displays the content on the UI.

---

## 🚀 Features

- Fetches data from the Lorem Ipsum API
- Uses Redux for state management
- Handles loading state before data is fetched
- Displays Title and Body in a readable format
- Data is rendered inside an HTML `<p>` element
- Built with React + Vite

---

## 🔗 API Used

